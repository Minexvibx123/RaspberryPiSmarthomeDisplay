# HomePanel – Python-Abhängigkeiten
#
# Installation:  .venv/bin/pip install -r requirements.txt
#
# Systempakete (Wayland-Kiosk: cage, wlr-randr, xwayland; Qt/PySide6-
# Laufzeitbibliotheken) installiert automatisch:  scripts/install.sh

PySide6>=6.6
requests>=2.31
websocket-client>=1.7

# Optional: QtWebEngineWidgets (Web-Panel-Widget, interner Browser) ist
# normalerweise im PySide6-Wheel enthalten. Fehlt es auf der Plattform,
# degradieren beide Funktionen graceful, statt abzustürzen.
